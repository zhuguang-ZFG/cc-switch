# NewAPI DX pass — glm `[1M]`, thinking drain, Haiku/Agnes prices, weights

**Date:** 2026-07-25  
**Host:** Aliyun NewAPI (`aliyun.donglicao.com` / `47.112.162.80`)  
**Status:** Applied on live DB (backups under `/opt/new-api/backups/one-api.before-dx-fix-*` and `one-api.before-glm-no-think-*`)

## Problems found (do not call “healthy” without these)

1. **`glm-5.2[1M]`** — zero abilities + no ModelRatio → hard fail on gateway.
2. **GLM empty replies** — OpenAI-compat path burns almost all `max_tokens` as `reasoning_tokens`; Anthropic-shaped body returns empty `text` and `stop_reason=max_tokens` (matches “half answer / client_gone” reports).
3. **`agnes-2.0-flash`** — ability existed; price missing → “price not configured”.
4. **`claude-haiku-4-5-20251001`** — no ability (Claude Code dated Haiku id).
5. **Opus `#81`** — 6h consume avg ~52s / many >15s; still in pool at pri35.
6. **Zhipu `#42`** — glm avg ~21s vs `#41` ~2.6s with equal weight=0.

## Changes

### Pricing (`ModelRatio` / `CompletionRatio`)

- `glm-5.2[1M]` ← same as `glm-5.2`
- `agnes-2.0-flash`, `agnes-2.5-pro-alpha`
- Haiku aliases including `claude-haiku-4-5[1M]` / `[1m]` (dated id already priced)

### Routing

- `#41/#42/#123`: model + ability `glm-5.2[1M]` → map `glm-5.2`
- `#122`: abilities + map for `claude-haiku-4-5-20251001`, `claude-haiku-4-5[1M]` / `[1m]` → `agnes-2.0-flash`

### Zhipu thinking off

```json
{"enable_thinking": false}
```

Set as `channels.param_override` on `#41` and `#42`. Verified: `max_tokens=64` returns text `OK` with `reasoning_tokens=0`.

### Weights / priority

| Channel | Change |
|---------|--------|
| `#41` zhipu-glm-key2 | weight **50** |
| `#42` zhipu-glm-key1 | weight **8** |
| `#9` 100xlabs | weight **45** |
| `#10/#11/#60` | mid weights (28/24/30) |
| `#20` | weight **5** |
| `#81` | priority **20**, weight **1** (last resort) |

## Verification checklist

```text
POST /v1/messages  model=glm-5.2|glm-5.2[1M]|agnes-2.0-flash|claude-haiku-4-5-20251001|claude-opus-5[1M]
max_tokens=64  → non-empty text, no model_not_found / price errors
```

## Still open

- Opus upstream latency (black-box); weight only steers away from worst channels.
- Occasional slow `glm-5.2[1M]` (~30s) — watch hongshi vs zhipu.
- Many `status=2` dead channels (nvidia-nim, etc.) — clutter only until purged.
- Brief `system cpu overloaded` after NewAPI restart under load — avoid rapid restart loops.
