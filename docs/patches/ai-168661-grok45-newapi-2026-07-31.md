# ai.168661 grok-4.5 接入 NewAPI（ch39）（2026-07-31）

把 ai.168661.xyz 的 grok-4.5 接入 NewAPI（渠道 39），为 grok-4.5 增加第三个活跃源。AGENTS.md 记的「原 ai.168661 渠道15 停供 grok」为旧情况，实测该域名已恢复提供 grok-4.5。

## 1. 上游验证

- 端点 `https://ai.168661.xyz`，VPS（大陆阿里云）**可达**。
- `GET /v1/models`：`grok-4.3` / `grok-4.5` / `grok-chat-fast`。
- `grok-4.5` 实测返回 content + reasoning（2423 思维 token）+ prompt 缓存命中（cached_tokens=192），思维模型。
- **key 级上游限流**：刚接入时连续 429 `Upstream rate limit exceeded`（VPS 与本机同限，证明是 key 配额非 IP），等待 ~60s 后恢复并正常返回。属瞬时突发配额，会恢复。

## 2. NewAPI 渠道 ch39（sqlite 直写）

`POST /api/channel` 在本版本 panic，沿用 sqlite 直写（停容器 + channel_info BLOB）：

| 字段 | 值 |
|---|---|
| id | 39 |
| type | 1（OpenAI） |
| name | ai-168661-grok |
| base_url | `https://ai.168661.xyz`（不带 `/v1`，NewAPI 自动补） |
| models | grok-4.5 |
| group / priority / weight | default / 60 / 10 |
| auto_ban | 1（限流持续时自动禁用、恢复重开） |
| channel_info | 单 key BLOB |

grok-4.5 定价为全局每模型设置（ModelRatio=0.5/CompletionRatio=2/CacheRatio=0.1，已有，不因新增渠道改动）。改前备份 `one-api.before-ai168661-<ts>.db`。

接入后 grok-4.5 三活跃源：ch29 xiaoxiaobai-tailscale（w20）+ ch17 openoneapi（w10）+ ch39 ai-168661（w10）；ch20 fengwind 已禁用。

## 3. 验证

```text
grok-4.5 经 NewAPI 连发 4 次        -> 4/4 成功（content 正常，failover 兜底）
最近 40 次渠道分布                  -> ch17 ok=19 / ch29 ok=18 / ch39 ok=0
ch39 当前 0/2                        -> 瞬时 key 限流期，failover 到 ch17/29 正常；auto_ban 管理限流期
```

ch39 作为备级源：限流期 auto_ban 自动禁用、流量走 ch17/29；配额恢复后 auto_ban 重开，分担 grok-4.5 流量。

> 注：grok-4.5 是思维模型，小 max_tokens 会被思维链占满导致 content 空，调用给足 max_tokens（≥256）。

> 安全：本文档不含 ai.168661 API key、VPS 密码。
