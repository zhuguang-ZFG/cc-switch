# 7758 渠道接入（2026-08-14）

在本地 NewAPI（127.0.0.1:3002）新增 **ch82 `7758`**，上游 `https://7758.2149701.xyz`（Claude Bedrock + GPT 聚合中转：响应 model 字段回显 `CLAUDE_OPUS_47_BEDROCK` / `GPT_56_LUNA` 等内部名）。单 key（用户提供），key 不落 repo/文档。

## 渠道参数

| 项 | 值 |
|----|----|
| id / name | 82 / `7758` |
| type | 1（OpenAI） |
| base_url | `https://7758.2149701.xyz`（根路径，勿带 `/v1`） |
| models | `claude-opus-4.7,claude-opus-4.8,gpt-5.4,gpt-5.5,gpt-5.6-luna,gpt-5.6-sol,gpt-5.6-terra` |
| priority / weight | 50 / 5（新源小权重先例） |
| status | 1 enabled，auto_ban=1 |
| header_override | 无（curl/Go 任意 UA 直连均 200，无 muyuan 式 UA 白名单） |
| model_mapping | 无（原生名直挂） |

abilities 7 条已同步（enabled=1, p50/w5）。

## 路由影响（接入前核查）

- **独占承载**（NewAPI 无其他 ability）：`claude-opus-4.7`、`claude-opus-4.8`、`gpt-5.6-luna`；`gpt-5.4`/`gpt-5.6-terra` 仅 banned ch70 持有 → ch82 为唯一 enabled。
- **gpt-5.5**：与 muyuan ch80 同层 p50/w5 加权互备（实测两侧均命中）。
- **gpt-5.6-sol**：ch82 成为主层唯一 enabled p50 承载（原 p50 层 ch2/ch30/ch70 全 banned/disabled），agentrouter ch45 保持 p40 兜底。观测：ch82 建立后 sol 流量 2 次落 ch82、1 次落 ch45（15:27:49Z）——判定为慢请求重试降层（本人 e2e sol 耗时 13s），优先级语义本身正常（大优先，见 codex-relay runbook）。guardian auto_ban 路径兜底。

## 上游特征（接入前直连实测）

- `/v1/models` 200，7 模型；全部 chat 非流式 200（3.5~20.8s），流式 SSE 200（2.7~16.2s），content/finish=stop 正常。
- 无 UA 限制、无配额报错（对比 muyuan key A 的 403 quota 与 client_restricted）。
- Claude 两模型经 OpenAI chat 格式互转正常（`msg_*` id + `CLAUDE_OPUS_*_BEDROCK` 回显 → AWS Bedrock 上游）。

## 验证

- NewAPI 渠道测试：7/7 模型 success（2.5s~9.9s）。
- 网关 e2e 非流式：7/7 HTTP 200，content/finish 正常。
- 网关流式 e2e（OMP 注册的 4 模型）：均 `text/event-stream` 200，chunks 4~5、finish=stop。
- 日志归因：独占模型全部落 ch82；gpt-5.5 双渠分担；sol 主层 ch82 + 重试降层 ch45 各见。
- OMP 探针（`omp -p --model`）：`zg-newapi/gpt-5.4`、`gpt-5.6-luna`、`gpt-5.6-terra`、`claude-opus-4.7` 全部返回预期 token。首轮批量探针 3 空输出为 `tail -1` 抓到尾行空白的采集伪影，逐一复测全过。

## OMP 注册（zg-newapi provider）

限额来源 = **pi.dev/models 官方注册表**（OMP 自身 convention 源），非类比继承：

| selector | context | maxTokens | 源 |
|----------|--------:|----------:|----|
| `gpt-5.4` | 272,000 | 128,000 | pi.dev/models/openai/gpt-5-4 |
| `gpt-5.6-luna` | 272,000 | 128,000 | pi.dev/models/openai/gpt-5-6-luna |
| `gpt-5.6-terra` | 272,000 | 128,000 | pi.dev/models/openai/gpt-5-6-terra |
| `claude-opus-4.7` | 1,000,000 | 128,000 | pi.dev/models/anthropic/claude-opus-4-7 |

四模型注册表均标 reasoning=yes、input=text+image；**image 输入经 7758 中转未实测，OMP 侧暂未声明 image**（遵循"验证才声明"先例）。注意注册表值与聚合商口径有出入（requesty/howaiworks 称 GPT-5.5/5.6 为 1.05M ctx）——以 OMP 自身注册表为准。`claude-opus-4.8` 未注册：已有 `claude-opus-4-8`（连字符命名，anthropic gateway 路径）selector。

备份：`~/.omp/agent/models.yml.20260814-232920-7758.bak`。

## 备份与回滚

- DB 快照：`~/.new-api-local/backups/new-api-before-7758-channel-20260814-232458.db`（serialize 快照，integrity_check=ok，64.8MB）。
- 回滚：`POST /api/channel/82/status {"status":2}` 禁用（sol 主层回落 agentrouter 兜底、gpt-5.5 回落 muyuan 单载），或 `DELETE /api/channel/82`；OMP 侧恢复 models.yml 备份。
